//Set Character -- Play Theme

// Play level theme.
audio_play_sound(snd_brokeback, 100, true);

//Set Nate Character
global.playerChar = 1;

//Set timer for level
with (obj_player){
    myTime = 300;
}